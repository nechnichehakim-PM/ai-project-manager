{
  "name": "hub-unified",
  "version": "2.0.0",
  "description": "Hub Unifie - PM + Delivery/Business Platform",
  "private": true,
  "scripts": {
    "dev": "npx serve public/app -p 3000 --no-clipboard",
    "build": "node scripts/build.js",
    "start": "npx serve public/app -p 3000 --no-clipboard"
  }
}
